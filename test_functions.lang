lang.name = Lang Tests (Functions)
lang.version = v1.0.0

lang.test = 1

# Preparation
# ...

# Tests
fn.testUnit(Functions)
{
	fn.testSubUnit(Lang functions [func.isLangVersionNewer])
	
	fn.testAssertStrictEquals(func.isLangVersionNewer(), 0)
}
{
	fn.testSubUnit(Lang functions [func.isLangVersionOlder])
	
	fn.testAssertStrictEquals(func.isLangVersionOlder(), 0)
}
{
	fn.testSubUnit(System functions [func.sleep])
	
	$t1 = func.currentTimeMillis()
	func.sleep(5)
	$t2 = func.currentTimeMillis()
	$td $= $t2 - $t1
	
	fn.testAssertGreaterThanOrEquals($td, 5)
	fn.testAssertLessThan($td, 8)
}
{
	fn.testSubUnit(System functions [func.currentTimeMillis])
	
	$t1 = func.currentTimeMillis()
	
	fn.testAssertStrictEquals(func.typeOf($t1), $LANG_TYPE_LONG) # TODO replace type check with lang test function
	
	$t2 = func.currentTimeMillis()
	
	fn.testAssertLessThanOrEquals($t1, $t2)
}
