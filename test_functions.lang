lang.name = Lang Tests (Functions)
lang.version = v1.0.0

lang.test = 1

# Preparation
# ...

# Tests
{
	func.langTestSubUnit(Lang functions [func.isLangVersionNewer])
	
	func.langTestAssertStrictEquals(func.isLangVersionNewer(), 0)
}
{
	func.langTestSubUnit(Lang functions [func.isLangVersionOlder])
	
	func.langTestAssertStrictEquals(func.isLangVersionOlder(), 0)
}
{
	func.langTestSubUnit(System functions [func.sleep])
	
	$t1 = func.currentTimeMillis()
	func.sleep(5)
	$t2 = func.currentTimeMillis()
	$td $= $t2 - $t1
	
	func.langTestAssertGreaterThanOrEquals($td, 5)
	func.langTestAssertLessThan($td, 8)
}
{
	func.langTestSubUnit(Error functions [func.currentTimeMillis])
	
	$t1 = func.currentTimeMillis()
	
	func.langTestAssertStrictEquals(func.typeOf($t1), $LANG_TYPE_LONG) # TODO replace type check with lang test function
	
	$t2 = func.currentTimeMillis()
	
	func.langTestAssertLessThanOrEquals($t1, $t2)
}
